# 编译产物（中间文件）
*.o
*.obj
*.out
*.elf
*.bin
*.hex
*.map

# 注意：demo.exe 故意保留在仓库中（学生可直接双击运行）
# 不要把 *.exe 加到这里

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# 临时文件
*.tmp
*.bak

# mdbook 临时构建产物（每次 build 重新生成）
_build/
book/_book/
.mdbook-cache/

# _book/ 是 gh-pages 分支的 git worktree 挂载点
# 不在 master 跟踪，git worktree 自己管理
_book/

# Linux 内核源码（v6.6 LTS，本地教学参考用·不推送到 remote）
/linux-kernel/

# 私有部署/构建脚本（作者自己用·不发布给读者）
/build.bat
/deploy.bat
/deploy.ps1
/preview.bat
/push.bat

# 封面素材源文件（HTML 切图源·不发布·只在仓库本地用）
/cover_publication*.html
